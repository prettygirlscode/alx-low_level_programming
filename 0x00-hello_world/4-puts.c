/*
 * A c program that prints 'programming is like building a multilingual puzzle'
 * without using the 'printf' function
 */
#include <stdio.h>
#include <stdlib.h>
/**
 * main - Entry point
 * Despription: 'printing a string without using the printf function
 * Return: Always 0 (Success)
 */
int main(void)
{
	char str[] = "\"Programming is like building a multilingual puzzle";

	puts(str);

	return (0);
}
